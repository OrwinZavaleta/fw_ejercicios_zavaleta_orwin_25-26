services:
  mi-app-web:
    build: .
    container_name: app-cliente-orwin
    restart: unless-stopped
    environment:
      - VIRTUAL_HOST=cliente.orwinzavaleta.dpdns.org
      - VIRTUAL_PORT=80
    networks:
      - apps-net

networks:
  apps-net:
    external: true